# 通用方案优于兼容 hack

设计过滤/分析方案时，选择语言/项目类型无关的通用机制，特定项目需求走配置文件。

## 实例

R100 报告中 .pbxproj / emoji.json / DoKit 等噪声 → 不硬编码排除 iOS 特定文件，而是设计 L4 语言过滤（自动检测主语言，排除非源码）+ .decayrc 配置覆盖。

## 决策规则

1. 先问"这个问题在其他项目类型中对应什么"
2. 设计语言无关的通用方案
3. 边缘 case 通过 .decayrc 配置解决
