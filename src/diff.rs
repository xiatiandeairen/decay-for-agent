use crate::config::Thresholds;
use crate::types::{DiffEntry, Snapshot};

pub fn diff(_prev: &Snapshot, _curr: &Snapshot, _thresholds: &Thresholds) -> Vec<DiffEntry> {
    todo!()
}

pub fn _stub() {}
