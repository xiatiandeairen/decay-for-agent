use std::path::Path;

use crate::error::Result;
use crate::types::Function;

#[allow(clippy::missing_errors_doc)]
pub fn scan(_project_root: &Path) -> Result<Vec<Function>> {
    todo!()
}

pub fn _stub() {}
