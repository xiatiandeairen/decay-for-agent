use std::path::{Path, PathBuf};

use crate::error::Result;

#[allow(clippy::missing_errors_doc)]
pub fn walk_rust_files(_project_root: &Path) -> Result<Vec<PathBuf>> {
    todo!()
}

pub fn _stub() {}
